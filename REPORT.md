# What and why:

Built here is an Aquarium hobbyist information site. The goal is to provide users, whether experienced fish keepers, or new to the hobby, with knowledge regarding the care necessary in maintaining a healthy, aquatic environment. User's may use the site to access information regarding tank stocking, compatible species, general maintenance, setup guides, aqua-scaping ideas, or just to ask the site's LLM for anything marine related.

Regarding the AI's behavior, the role of the AI is to provide accurate information specific to fish keeping. It should gather from the `knowledge_base`, using RAG to reference + gather the relevant info, and response in accordance with the user's input. Some difficulties, however, in getting the AI to behave go as follow:

    1. There's the general risk of bad advice, or misinformation being generated by the LLM.
    2. There are a huge variety of fish, plants, water types, locations, pricing, etc. etc., that the user could ask about, for the LLM to ascertain from - info mix ups could occur.
    3. In addition, due to various species, and their various varieties within (EX: Over 2,000 varieties of freshwater Minnow fish alone), the LLM can be too broad in its output, leading to users feeling gainless.
    4. The LLM has rules / keywords to follow by, to prevent users from prompting irrelevant topics, but this in turn could limit acceptable user input.

A lot of these errors occurred during the first iterations of the site.

#
# Iteration V1: Code implementation, website setup, project scope.

**Change:** Baseline, the implementation of the site's backend, frontend, and eval codebases. A document of the project's scope was provided to Kiro (found in `kiro_prompt_docs`).

**Motivating example:** To provide a plan for project implementation, the scope size, and whether all features suggest thus far are possible to include.

**Delta:** Eval wasn't ran for the first iteration of the site. Due to errors in running `eval.py`, and site API issues. However, since the site's functions returned errors, the score can be 0 / 12 tests passed.

**Conclusion:** The site hosted locally, layout was there, buttons worked, the user is able to interact with the features and the LLM. However, everything, besides the water volume calculator, returned errors. Next step is to fix these issues.





#
# Iteration V2: Bug fixes, simplification, keyword extraction, true testing.

**Change:** Bug fixes to the site's features. Simplified README.md instructions for easier startup. Applied a document extractor `ingest.py`, for info files to be manually inserted (for the LLM to read).

**Motivating example:** The errors the site returned didn't allow for proper usage of the features present. Once the features did work, the information returned didn't satisfy, and was slightly incorrect.

**Delta:** Eval returned a 12 / 12 passed score. Pretty impressive, but these test contained basic / safe prompts.

**Conclusion:** Test will need to be a little harder / more in-depth with the site's complex features. More aquarium data would need to be implemented, to expand the LLM's responses. 





#
# Iteration V3: Database upgrades, `eval.py` rebuilt, test cases rebuilt.

**Change:** Added `links.txt`, that provides manually inserted URLs to relevant topics, mainly from wiki. Previous test cases did not challenge the site's features; they were too safe, too basic. 

**Motivating example:** The website covers a topic of endless variety, more so with the addition of external sites. I want the testing of features and responses to be complex, and inclusive, for the changing landscape of the website, and its database.

**Delta:** The new `eval.py` file can better test the site, with and without calls to the LLM. However, running the `eval.py` file resulted in a score of 16 / 44 total, 0 failed, with 28 tests skipped.

**Conclusion:** Changes to the LLM's responses haven't occurred yet, so it still returns its basic responses. I suspect that may be why many tests were skipped. Next features are to update the LLM's role in the site.





#
# Iteration V4: Upgraded site features, upgraded LLM responses.

**Change:** 

**Motivating example:** 

**Delta:** 

**Conclusion:** 




#
# Iteration V5: Proof of concept in motion.

**Change:**

**Motivating example:**

**Delta:**

**Conclusion:**




#
# Code Walkthrough

    Trace one user action through your code with file:line references. Explain one design decision and one alternative you considered and rejected. Generic "this calls the API" descriptions don't earn full credit.

After initial generation of the backend, I request for a specific file, `ingest.py` to be created, 

Design decision:

    I want for the LLM to extract from above documents, to provide the user with accurate information.

Alternative considered and rejected:

    Kiro provided 



`eval.py`

First versions were basic, 

I rejected it, and wanted it to be more complex - to truly test the website, its features, and its LLM role.






        def fetch_url(url: str) -> tuple[str, str]:
        """
        Fetch a web page and extract its readable text content.

        Uses requests to download the HTML and BeautifulSoup to strip tags,
        navigation, ads, and boilerplate — keeping only the main article text.

        Returns:
            (page_title, plain_text) — both strings.
            Returns ("", "") on any error.
        """
        try:
            import requests
        except ImportError:
            print("  [ERROR] requests not installed. Run: pip install requests")
            return "", ""

        try:
            from bs4 import BeautifulSoup
        except ImportError:
            print("  [ERROR] beautifulsoup4 not installed. Run: pip install beautifulsoup4")
            return "", ""

        try:
            print(f"  [FETCH] {url}")
            resp = requests.get(url, headers=HTTP_HEADERS, timeout=15)
            resp.raise_for_status()   # Raise on 4xx/5xx responses
        except Exception as exc:
            print(f"  [ERROR] Could not fetch {url}: {exc}")
            return "", ""

        # Parse the HTML
        soup = BeautifulSoup(resp.text, "html.parser")

        # Extract the page title
        title_tag = soup.find("title")
        page_title = title_tag.get_text(strip=True) if title_tag else urlparse(url).netloc

        # Remove elements that are never useful content:
        # scripts, styles, navigation, headers, footers, ads, sidebars
        for tag in soup(["script", "style", "nav", "header", "footer",
                        "aside", "form", "button", "iframe", "noscript",
                        "meta", "link", "figure"]):
            tag.decompose()   # Remove the tag and its children from the tree

        # Try to find the main content area first (common article containers)
        main_content = (
            soup.find("article") or
            soup.find("main") or
            soup.find(id=re.compile(r"content|main|article|body", re.I)) or
            soup.find(class_=re.compile(r"content|main|article|post|entry", re.I)) or
            soup.find("body") or
            soup
        )

        # Extract text with newlines between block elements
        text = main_content.get_text(separator="\n", strip=True)

        # Clean up excessive whitespace and blank lines
        lines = [line.strip() for line in text.splitlines()]
        lines = [line for line in lines if line]   # Remove empty lines
        clean_text = "\n\n".join(lines)

        return page_title, clean_text





#
# AI disclosure & safety

    How you used your AI coding assistant (Kiro or otherwise), including 2–3 specific moments it failed and how you recovered. Then 2–3 sentences naming one safety risk specific to your app and the mitigation or accepted limit you chose. Common candidates: prompt injection, hallucination harm, PII exposure, biased output, cost runaway.


Kiro played a huge role in the development of the backend and frontend of the site. 


Failures:

    The knowledge base generated however, I'd say was one of Kiro's inefficiencies. Upon first iteration, the AI implemented very basic, pre-generated responses to key-specific user inputs.

        INPUT: "Guppy"

        OUTPUT: Basic response about water parameters needed, tank space, and their behavior.
        
    While the response comes out consistently (same every time), what I want is more freedom in what the user can input, and a more varied, yet informational accurate response from the LLM call.


Risks:

    One safety risk, which could affect user's approval of the site, and the livelihood of the aquatic environment itself, is misinformation / bad advice given by the AI models.

